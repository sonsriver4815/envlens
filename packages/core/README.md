# @configenvy/core

Core scanning and diagnostic utilities for `configenvy`.

Most users should install the CLI package:

```bash
npm install -D configenvy
```

See the full documentation at [github.com/sonsriver4815/configenvy](https://github.com/sonsriver4815/configenvy).
